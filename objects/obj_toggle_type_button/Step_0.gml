if (!global.is_selecting_panel) {
	switch (set_variable) {
		case "show_damageIndicator":
			is_toggle = global.show_damageIndicator;
		break;
		case "show_currencyIndicator":
			is_toggle = global.show_currencyIndicator;
		break;
	}
}